pipeline {
    agent none 
    tools {
        maven 'M2_HOME'
    }
    stages {
        stage('maven Commands') {
            agent any
            steps {
                node {
                    sh 'mvn clean compile install package'
                }
            }
        }
    }
}
