pipeline {
    agent none 
    tools {
        maven 'M2_HOME'
    }
    stages {
        stage('maven Commands') {
            agent any
            steps {
                    sh 'mvn clean compile install package'
                }
            }
        }
    }
}
