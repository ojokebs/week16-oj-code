pipeline {
    agent none 
    tools {
        maven 'M2_HOME'
    }
    stages {
        stage('mvn verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=ojokebs_week16-oj-code') {
            agent any
            steps {
                    sh 'mvn clean compile install package'
                }
            }
        }
    }

